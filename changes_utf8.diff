diff --git a/app.js b/app.js
index 41d29a2..2dcba50 100644
--- a/app.js
+++ b/app.js
@@ -9,9 +9,7 @@ const state = {
     userMarker: null,
     userCircle: null,
     userLocation: null, // {lat, lng}
-    showHighLevel: true,
-    showMidLevel: true,
-    showLowLevel: false,
+    showMore: false,
     lastFilteredResults: []
 };
 
@@ -29,6 +27,13 @@ const attributeLabels = {
     kid_noise_tolerant: '銝?摮拙'
 };
 
+const levelLabels = {
+    '擃?: '?? ?拙?撣嗅?摮?,
+    '銝?: '?? ?臭誑?',
+    '???': '?? ???',
+    '鞈?銝雲': '??鞈?頛?'
+};
+
 // DOM Elements
 const restaurantList = document.getElementById('restaurant-list');
 const homeView = document.getElementById('home-view');
@@ -51,12 +56,13 @@ const locAllBtn = document.getElementById('loc-all');
 const locChips = document.querySelectorAll('.loc-chip');
 
 
-window.toggleCategory = function(level) {
-    if (level === 'high') state.showHighLevel = !state.showHighLevel;
-    if (level === 'mid') state.showMidLevel = !state.showMidLevel;
-    if (level === 'low') state.showLowLevel = !state.showLowLevel;
-    renderList();
-};
+function resetViewState() {
+    state.showMore = false;
+    const mapStatus = document.getElementById('map-status');
+    if (mapStatus) {
+        mapStatus.textContent = '?桀?憿舐內頛?葆撠酋??撱?;
+    }
+}
 
 // Initialization
 function init() {
@@ -117,6 +123,7 @@ function setupEventListeners() {
     // Location Selection Logic
     if (locAllBtn) {
         locAllBtn.addEventListener('click', () => {
+            resetViewState();
             state.locations.clear();
             clearUserLocation();
             updateModalUI();
@@ -125,6 +132,7 @@ function setupEventListeners() {
 
     locChips.forEach(chip => {
         chip.addEventListener('click', () => {
+            resetViewState();
             const loc = chip.dataset.loc;
             if (state.locations.has(loc)) {
                 state.locations.delete(loc);
@@ -166,6 +174,7 @@ function setupEventListeners() {
 }
 
 function toggleFilter(filterAttr) {
+    resetViewState();
     if (state.filters.has(filterAttr)) {
         state.filters.delete(filterAttr);
     } else {
@@ -201,6 +210,7 @@ function formatDistance(km) {
 
 function renderList() {
     restaurantList.innerHTML = '';
+    resultsCount.innerHTML = '';
     resultsCount.style.display = 'block';
 
     const selectedFilters = Array.from(state.filters);
@@ -214,89 +224,106 @@ function renderList() {
         return { ...res, distance: dist };
     });
 
-    // 2. Filter out based on global exclusions and initial filtering
+    // 2. Filter data
     let eligibleData = dataWithDistance.filter(res => {
-        // District filter
         if (state.locations.size > 0) {
             const hasMatch = Array.from(state.locations).some(loc => res.address.includes(loc));
             if (!hasMatch) return false;
         }
-
-        // Distance filter (only show restaurants within 3km if nearby is active)
-        if (state.userLocation && res.distance > 3) {
-            return false;
-        }
-
-        // Exclude: ALL tags are Unknown (no information at all) -> Wait, we want to allow them if showLowLevel is true
-        // But for explicit filters:
+        if (state.userLocation && res.distance > 3) return false;
+        
         const meetsFilters = selectedFilters.every(f => res.attributes[f] === 'yes');
         if (selectedFilters.length > 0 && !meetsFilters) return false;
 
         return true;
     });
 
-    // Sort by distance (nearest first), or if no distance, just by parent_friendly_score desc
+    // Sort
+    const levelWeight = { '擃?: 4, '銝?: 3, '???': 2, '鞈?銝雲': 1 };
     eligibleData.sort((a, b) => {
-        if (state.userLocation) {
-            return a.distance - b.distance;
-        }
+        const weightA = levelWeight[a.parent_friendly_level] || 0;
+        const weightB = levelWeight[b.parent_friendly_level] || 0;
+        if (weightA !== weightB) return weightB - weightA;
+        if (state.userLocation) return a.distance - b.distance;
         return (b.parent_friendly_score || 0) - (a.parent_friendly_score || 0);
     });
 
-    // Group into High, Mid, Low
-    const highData = eligibleData.filter(r => r.parent_friendly_level === '擃?);
-    const midData = eligibleData.filter(r => r.parent_friendly_level === '銝?);
-    const lowData = eligibleData.filter(r => r.parent_friendly_level === '鞈?銝雲');
+    // Split into Groups
+    // Recommended: High or Medium
+    const recommended = eligibleData.filter(r => r.parent_friendly_level === '擃? || r.parent_friendly_level === '銝?);
+    // Others: Insufficient Info or Needs Attention
+    const others = eligibleData.filter(r => r.parent_friendly_level === '鞈?銝雲' || r.parent_friendly_level === '???');
 
-    if (highData.length === 0 && midData.length === 0 && lowData.length === 0) {
+    if (recommended.length === 0 && others.length === 0) {
         renderEmptyState();
-        resultsCount.innerHTML = `
-            <div style="background: var(--card-bg); padding: 1rem; border-radius: 1rem; box-shadow: 0 1px 2px rgba(0,0,0,0.05); margin-bottom: 0.5rem; font-weight: 600; line-height: 1.6;">
-                <div style="display: flex; align-items: center;"><div style="display:inline-block; width:10px; height:10px; background:#4FB3AA; border-radius:50%; margin-right:0.5rem;"></div>?拙?撣嗅?摮抬?0 ??/div>
-                <div style="display: flex; align-items: center;"><div style="display:inline-block; width:10px; height:10px; background:#FFB347; border-radius:50%; margin-right:0.5rem;"></div>?航?拙?嚗? ??/div>
-                <div style="display: flex; align-items: center;"><div style="display:inline-block; width:10px; height:10px; background:#CBD5E1; border-radius:50%; margin-right:0.5rem;"></div>鞈?頛?嚗? ??/div>
-            </div>
-        `;
         renderMap([]);
         return;
     }
 
+    // Update Result Count
     resultsCount.innerHTML = `
-        <div style="background: var(--card-bg); padding: 0.5rem; border-radius: 1rem; box-shadow: 0 1px 2px rgba(0,0,0,0.05); margin-bottom: 0.5rem; font-weight: 600; line-height: 1.6;">
-            <div onclick="toggleCategory('high')" style="cursor: pointer; display: flex; align-items: center; justify-content: space-between; padding: 0.5rem; border-radius: 0.5rem; transition: background 0.2s;" onmouseover="this.style.background='#f1f5f9'" onmouseout="this.style.background='transparent'">
-                <div style="display: flex; align-items: center;"><div style="display:inline-block; width:10px; height:10px; background:#4FB3AA; border-radius:50%; margin-right:0.5rem;"></div>?拙?撣嗅?摮抬?${highData.length} ??/div>
-                <div style="color: var(--text-muted);">${state.showHighLevel ? '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"></path><circle cx="12" cy="12" r="3"></circle></svg>' : '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17.94 17.94A10.07 10.07 0 0 1 12 20c-7 0-11-8-11-8a18.45 18.45 0 0 1 5.06-5.94M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 8 11 8a18.5 18.5 0 0 1-2.16 3.19m-6.72-1.07a3 3 0 1 1-4.24-4.24"></path><line x1="1" y1="1" x2="23" y2="23"></line></svg>'}</div>
-            </div>
-            <div onclick="toggleCategory('mid')" style="cursor: pointer; display: flex; align-items: center; justify-content: space-between; padding: 0.5rem; border-radius: 0.5rem; transition: background 0.2s;" onmouseover="this.style.background='#f1f5f9'" onmouseout="this.style.background='transparent'">
-                <div style="display: flex; align-items: center;"><div style="display:inline-block; width:10px; height:10px; background:#FFB347; border-radius:50%; margin-right:0.5rem;"></div>?航?拙?嚗?{midData.length} ??/div>
-                <div style="color: var(--text-muted);">${state.showMidLevel ? '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"></path><circle cx="12" cy="12" r="3"></circle></svg>' : '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17.94 17.94A10.07 10.07 0 0 1 12 20c-7 0-11-8-11-8a18.45 18.45 0 0 1 5.06-5.94M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 8 11 8a18.5 18.5 0 0 1-2.16 3.19m-6.72-1.07a3 3 0 1 1-4.24-4.24"></path><line x1="1" y1="1" x2="23" y2="23"></line></svg>'}</div>
-            </div>
-            <div onclick="toggleCategory('low')" style="cursor: pointer; display: flex; align-items: center; justify-content: space-between; padding: 0.5rem; border-radius: 0.5rem; transition: background 0.2s;" onmouseover="this.style.background='#f1f5f9'" onmouseout="this.style.background='transparent'">
-                <div style="display: flex; align-items: center;"><div style="display:inline-block; width:10px; height:10px; background:#CBD5E1; border-radius:50%; margin-right:0.5rem;"></div>鞈?頛?嚗?{lowData.length} ??/div>
-                <div style="color: var(--text-muted);">${state.showLowLevel ? '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"></path><circle cx="12" cy="12" r="3"></circle></svg>' : '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17.94 17.94A10.07 10.07 0 0 1 12 20c-7 0-11-8-11-8a18.45 18.45 0 0 1 5.06-5.94M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 8 11 8a18.5 18.5 0 0 1-2.16 3.19m-6.72-1.07a3 3 0 1 1-4.24-4.24"></path><line x1="1" y1="1" x2="23" y2="23"></line></svg>'}</div>
-            </div>
+        <div style="font-weight: 800; font-size: 1.1rem; color: var(--text-main); margin-bottom: 0.5rem;">
+            ?曉 ${recommended.length} ??葆撠酋??撱?         </div>
     `;
 
-    // Render Data based on toggles
-    if (state.showHighLevel && highData.length > 0) {
-        highData.forEach(res => renderCard(res));
+    // Render Recommended Section
+    if (recommended.length > 0) {
+        const recSection = document.createElement('div');
+        recSection.className = 'results-section';
+        recSection.innerHTML = `<div class="section-header">?刻蝯虫?</div>`;
+        const recList = document.createElement('div');
+        recommended.forEach(res => renderCard(res, recList));
+        recSection.appendChild(recList);
+        restaurantList.appendChild(recSection);
     }
-    if (state.showMidLevel && midData.length > 0) {
-        midData.forEach(res => renderCard(res));
-    }
-    if (state.showLowLevel && lowData.length > 0) {
-        lowData.forEach(res => renderCard(res));
+
+    // Render Others Section (Hidden by default)
+    if (others.length > 0) {
+        const othersSection = document.createElement('div');
+        othersSection.className = 'results-section';
+        
+        const showMoreBtn = document.createElement('button');
+        showMoreBtn.className = 'show-more-btn';
+        showMoreBtn.id = 'show-more-others';
+        showMoreBtn.innerHTML = `
+            <span>?? 憿舐內?游??賊? (?怨?閮?撠????)</span>
+            <span class="arrow-icon">??/span>
+        `;
+        
+        const othersContainer = document.createElement('div');
+        othersContainer.className = 'collapsible-content';
+        othersContainer.id = 'others-list-container';
+        
+        othersSection.appendChild(showMoreBtn);
+        othersSection.appendChild(othersContainer);
+        
+        if (state.showMore) {
+            othersContainer.classList.add('expanded');
+            showMoreBtn.classList.add('active');
+            showMoreBtn.querySelector('span').textContent = '?嗅?憿??賊?';
+        }
+        
+        others.forEach(res => renderCard(res, othersContainer));
+        restaurantList.appendChild(othersSection);
+
+        showMoreBtn.addEventListener('click', () => {
+            state.showMore = !state.showMore;
+            othersContainer.classList.toggle('expanded', state.showMore);
+            showMoreBtn.classList.toggle('active', state.showMore);
+            
+            const mapStatus = document.getElementById('map-status');
+            if (mapStatus) {
+                mapStatus.textContent = state.showMore ? '?桀?憿舐內???撠??? : '?桀?憿舐內頛?葆撠酋??撱?;
+            }
+            showMoreBtn.querySelector('span').textContent = state.showMore ? '?嗅?憿??賊?' : '?? 憿舐內?游??賊? (?怨?閮?撠????)';
+            
+            renderMap(state.lastFilteredResults);
+        });
     }
 
     updateFilterSummary(eligibleData.length);
-
-    // Store results for map persistence
-    let mapData = [];
-    if (state.showHighLevel) mapData = mapData.concat(highData);
-    if (state.showMidLevel) mapData = mapData.concat(midData);
-    if (state.showLowLevel) mapData = mapData.concat(lowData);
-    state.lastFilteredResults = mapData;
+    state.lastFilteredResults = eligibleData;
     renderMap(state.lastFilteredResults);
 }
 
@@ -323,43 +350,108 @@ function updateFilterSummary(totalCount) {
     summaryCount.textContent = totalCount;
 }
 
-function renderCard(res) {
+function renderCard(res, container) {
     const card = document.createElement('div');
     card.className = 'restaurant-card';
+    card.id = `card-${res.place_id}`;
+
+    const isNeedsAttention = res.parent_friendly_level === '???';
 
     let distHtml = '';
     if (res.distance && res.distance !== Infinity) {
         distHtml = `<span class="distance-badge">? ${formatDistance(res.distance)}</span>`;
     }
 
-    const level = res.parent_friendly_level || '鞈?銝雲';
-    const levelClass = level === '擃? ? 'level-high' : (level === '銝? ? 'level-mid' : 'level-low');
+    // Identify Positive Tags and Negative Warnings
+    let positiveTags = [];
+    let warnings = [];
     
-    let positiveAttributes = [];
     Object.keys(res.attributes || {}).forEach(attr => {
-        if (res.attributes[attr] === 'yes' && attributeLabels[attr]) {
-            positiveAttributes.push(attributeLabels[attr]);
+        if (res.attributes[attr] === 'yes') {
+            positiveTags.push({ id: attr, label: attributeLabels[attr], icon: attributeIcons[attr] });
+        } else if (res.attributes[attr] === 'no') {
+            const warningLabel = attr === 'high_chair_available' ? '?芣?靘?蝡交?' : 
+                                 attr === 'spacious_seating' ? '蝛粹??航頛??? : 
+                                 attr === 'kids_menu' ? '?芣?靘?蝡仿?' : 
+                                 attr === 'kid_noise_tolerant' ? '?啣????? : '';
+            if (warningLabel) {
+                warnings.push(warningLabel);
+            }
         }
     });
-    const reasonText = positiveAttributes.length > 0 ? positiveAttributes.join('??) : '?桀?蝻箔??Ⅱ?扛摮???閮?;
+
+    const warningsHtml = warnings.length > 0 ? `<div class="warning-item">${warnings.join('??)}</div>` : '';
 
     card.innerHTML = `
-        <div style="display: flex; justify-content: space-between; align-items: flex-start;">
-            <div class="restaurant-name">${res.name}</div>
-            ${distHtml}
+        <!-- 1. Name Section -->
+        <div style="display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 0.5rem;">
+            <div class="restaurant-name" style="font-size: 1.1rem; margin-bottom: 0; line-height: 1.3;">${res.name}</div>
+            <div style="display: flex; align-items: center; gap: 0.5rem;">
+                ${distHtml}
+                <span style="font-size: 1.1rem; color: var(--text-muted); opacity: 0.4;">??</span>
+            </div>
         </div>
-        <div class="restaurant-rating" style="margin-bottom: 0.75rem;">潃?${res.rating}</div>
-        
-        <div class="decision-reason">
-            <span class="level-badge ${levelClass}">${level}</span>
-            <span class="reason-text">${reasonText}</span>
+
+        <!-- 2. Decision Group (Recommendation + Warnings/Tags) -->
+        <div style="margin-bottom: 0.65rem; display: flex; align-items: center; gap: 0.6rem;">
+            <!-- Badge -->
+            <div style="flex-shrink: 0;">
+                ${(() => {
+                    const level = res.parent_friendly_level;
+                    if (!levelLabels[level]) return '';
+                    let color = '#15803d';
+                    let bg = '#f0fdf4';
+                    let borderColor = 'transparent';
+                    if (level === '銝?) { color = '#16a34a'; bg = '#f0fdf4'; }
+                    if (level === '???') { color = '#ef4444'; bg = '#fef2f2'; borderColor = '#FECACA'; }
+                    if (level === '鞈?銝雲') { color = '#64748b'; bg = '#f8fafc'; borderColor = '#E2E8F0'; }
+                    return `<span class="decision-summary" style="color: ${color}; background: ${bg}; border: 1px solid ${borderColor};">${levelLabels[level]}</span>`;
+                })()}
+            </div>
+
+            <!-- Main Info (Warnings for NeedsAttention, Tags for others) -->
+            <div style="grid-row: 1; font-size: 0.85rem; font-weight: 700;">
+                ${isNeedsAttention ? warningsHtml : `
+                    <span style="color: #5fb3ad;">
+                        ${positiveTags.map(t => t.label).join('??)}
+                    </span>
+                `}
+            </div>
+        </div>
+
+        <!-- 3. Explanation Section (Summary) -->
+        <div class="card-summary" style="margin-bottom: 0.5rem; padding: 0.6rem 0.75rem;">
+            ${res.card_summary || '?桀?閬芸???鞈?頛???撱箄降?????摨振蝣箄???}
+        </div>
+
+        <!-- 4. Bottom Info Section (Rating & Address) -->
+        <div style="display: flex; align-items: center; gap: 0.8rem; font-size: 0.75rem; font-weight: 500; color: #64748b; opacity: 0.6;">
+            <div style="display: flex; align-items: center; gap: 0.2rem;">
+                <span style="color: #FFB800;">潃?/span> ${res.rating}
+            </div>
+            <div style="display: flex; align-items: center; gap: 0.2rem; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">
+                <span>??</span> ${res.address}
+            </div>
         </div>
-        
-        <div class="restaurant-address">?? ${res.address}</div>
     `;
 
-    card.addEventListener('click', () => showDetail(res));
-    restaurantList.appendChild(card);
+    card.addEventListener('click', (e) => {
+        // Highlight this card
+        document.querySelectorAll('.restaurant-card').forEach(c => c.classList.remove('highlighted'));
+        card.classList.add('highlighted');
+        
+        // If not clicking a specific action, show detail
+        showDetail(res);
+        
+        // Link to map
+        if (state.markers[res.place_id]) {
+            const marker = state.markers[res.place_id];
+            state.map.setView(marker.getLatLng(), 16);
+            marker.openPopup();
+        }
+    });
+
+    container.appendChild(card);
 }
 
 function renderEmptyState() {
@@ -419,6 +511,7 @@ function clearUserLocation() {
 }
 
 window.clearFilters = () => {
+    resetViewState();
     state.filters.clear();
     state.locations.clear();
     clearUserLocation();
@@ -451,34 +544,60 @@ function showDetail(restaurant) {
     }
 
     const level = restaurant.parent_friendly_level || '鞈?銝雲';
-    const levelClass = level === '擃? ? 'level-high' : (level === '銝? ? 'level-mid' : 'level-low');
+    const isRecommended = (level === '擃? || level === '銝?);
+    
+    let warningsHtml = '';
+    Object.keys(restaurant.attributes || {}).forEach(attr => {
+        if (restaurant.attributes[attr] === 'no') {
+            const warningLabel = attr === 'high_chair_available' ? '?芣?靘?蝡交?' : 
+                                 attr === 'spacious_seating' ? '蝛粹??航頛??? : 
+                                 attr === 'kids_menu' ? '?芣?靘?蝡仿?' : 
+                                 attr === 'kid_noise_tolerant' ? '?啣????? : '';
+            if (warningLabel) {
+                warningsHtml += `<div class="warning-item">?? ${warningLabel}</div>`;
+            }
+        }
+    });
 
     detailContent.innerHTML = `
         <h1 style="margin-bottom: 0.5rem; color: var(--text-main);">${restaurant.name}</h1>
         <div class="restaurant-rating" style="font-size: 1.1rem; margin-bottom: 0.5rem;">潃?${restaurant.rating}</div>
         <div class="restaurant-address" style="font-size: 0.9rem; margin-bottom: 1.5rem;">?? ${restaurant.address}</div>
         
-        <div style="font-weight: 700; margin-bottom: 1rem; color: var(--text-muted);">閬芸???閰</div>
-        <div class="decision-reason" style="margin-bottom: 1.5rem; font-size: 1.1rem;">
-            <span class="level-badge ${levelClass}" style="font-size: 0.9rem; padding: 0.4rem 0.8rem;">${level}</span>
+        <div style="font-weight: 700; margin-bottom: 1rem; color: var(--text-muted);">閬芸???撱箄降</div>
+        <div style="margin-bottom: 1.5rem;">
+            ${(() => {
+                const label = levelLabels[level];
+                if (!label) return '';
+                let color = '#15803d';
+                let bg = '#f0fdf4';
+                if (level === '銝?) { color = '#16a34a'; bg = '#f0fdf4'; }
+                if (level === '???') { color = '#ef4444'; bg = '#fef2f2'; }
+                if (level === '鞈?銝雲') { color = '#64748b'; bg = '#f8fafc'; }
+                return `<span style="padding: 0.4rem 0.8rem; border-radius: 0.5rem; font-weight: 800; font-size: 0.9rem; background: ${bg}; color: ${color};">${label}</span>`;
+            })()}
         </div>
         
-        <div style="font-weight: 700; margin-bottom: 1rem; color: var(--text-muted);">蝚血??</div>
+        ${warningsHtml ? `
+            <div style="font-weight: 700; margin-bottom: 1rem; color: var(--text-muted);">???鈭?</div>
+            <div class="warning-container" style="margin-bottom: 1.5rem;">${warningsHtml}</div>
+        ` : ''}
+
+        <div style="font-weight: 700; margin-bottom: 1rem; color: var(--text-muted);">閬芸???璇辣</div>
         <div class="tag-container" style="gap: 0.75rem; margin-bottom: 1.5rem;">
             ${tagsHtml}
         </div>
 
         <div class="ai-summary" style="margin-bottom: 1.5rem;">
             <div class="ai-summary-title">
-                AI?渡??扛摮擗?閮?+                閬芸??券???
                 <span class="info-icon" onclick="toggleDisclaimer(event)">??/span>
             </div>
             <div class="disclaimer-expandable" id="ai-disclaimer">
-                ?祈?閮蝟餌絞?寞?閰??芸??渡?嚗??? 5 ??嚗?質?撖阡????交?撌桃嚗遣霅唳??渲?閮?瑯?+                ?祆?閬蝟餌絞?渡?蝯?嚗遣霅唳??渲?閮?瑯?             </div>
-            <div class="ai-summary-text">${restaurant.ai_summary}</div>
+            <div class="ai-summary-text" style="font-size: 1rem; line-height: 1.7; color: var(--text-main);">${restaurant.ai_summary}</div>
         </div>
-
         ${signalsHtml}
 
         <button class="btn btn-primary" style="width: 100%; margin-top: 1rem; padding: 1.125rem; font-size: 1rem;" onclick="window.open('${getGoogleMapsUrl(restaurant)}', '_blank')">
@@ -641,10 +760,17 @@ function renderMap(restaurants) {
 
     restaurants.forEach(res => {
         if (res.latitude && res.longitude) {
-            // High=Green, Mid=Yellow, Low=Gray
-            let color = '#CBD5E1'; // Default Low
-            if (res.parent_friendly_level === '擃?) color = '#4FB3AA';
-            if (res.parent_friendly_level === '銝?) color = '#FFB347';
+            // Only show High/Medium by default
+            const level = res.parent_friendly_level || '鞈?銝雲';
+            if (!state.showMore && level !== '擃? && level !== '銝?) {
+                return;
+            }
+
+            // High=Dark Green, Mid=Light Green, Attention=Red, Info=Grey
+            let color = '#94a3b8'; // Default Grey (Insufficient Info)
+            if (level === '擃?) color = '#15803d'; // Dark Green
+            if (level === '銝?) color = '#86efac'; // Light Green
+            if (level === '???') color = '#ef4444'; // Red
             
             const marker = createMarker(res, color);
             marker.addTo(state.map);
@@ -686,15 +812,25 @@ function createMarker(res, color) {
     const popupContent = document.createElement('div');
     popupContent.className = 'map-popup-card';
     
-    const levelClass = res.parent_friendly_level === '擃? ? 'level-high' : (res.parent_friendly_level === '銝? ? 'level-mid' : 'level-low');
+    const level = res.parent_friendly_level || '鞈?銝雲';
+    const isRecommended = (level === '擃? || level === '銝?);
 
     popupContent.innerHTML = `
         <div class="map-popup-title">${res.name}</div>
-        <div style="margin-bottom: 0.5rem; display: flex; align-items: center; gap: 0.5rem;">
-            <span class="level-badge ${levelClass}" style="padding: 0.1rem 0.4rem; font-size: 0.7rem;">${res.parent_friendly_level || '鞈?銝雲'}</span>
-            <span style="font-size: 0.8rem;">潃?${res.rating}</span>
+        <div style="margin-bottom: 0.75rem; display: flex; align-items: center; gap: 0.5rem;">
+            ${(() => {
+                const label = levelLabels[level];
+                if (!label) return '';
+                let color = '#15803d';
+                let bg = '#f0fdf4';
+                if (level === '銝?) { color = '#16a34a'; bg = '#f0fdf4'; }
+                if (level === '???') { color = '#ef4444'; bg = '#fef2f2'; }
+                if (level === '鞈?銝雲') { color = '#64748b'; bg = '#f8fafc'; }
+                return `<span style="font-size: 0.75rem; font-weight: 700; padding: 0.15rem 0.4rem; border-radius: 0.4rem; background: ${bg}; color: ${color};">${label}</span>`;
+            })()}
+            <span style="font-size: 0.8rem; font-weight: 600; color: #64748b;">潃?${res.rating}</span>
         </div>
-        <button class="map-popup-btn" id="popup-btn-${res.place_id}">?亦?閰單?</button>
+        <button class="map-popup-btn" id="popup-btn-${res.place_id}">?亦?閰閰單?</button>
     `;
 
     // Handle click on "View Detail" button in popup
@@ -712,6 +848,7 @@ function createMarker(res, color) {
 }
 
 function handleGeolocation() {
+    resetViewState();
     if ("geolocation" in navigator) {
         navigator.geolocation.getCurrentPosition((position) => {
             const lat = position.coords.latitude;
diff --git a/build-ai-review-index.mjs b/build-ai-review-index.mjs
index 49650d0..72c2b7a 100644
--- a/build-ai-review-index.mjs
+++ b/build-ai-review-index.mjs
@@ -81,6 +81,7 @@ function buildRecord(placeId) {
     google_maps_url: googleMapsUrl,
     attributes: getAiAttributes(aiReview),
     ai_summary: aiReview.generated_summary || "",
+    card_summary: aiReview.card_summary || "",
     signals,
     parent_friendly_score: aiReview.parent_friendly_score ?? 0,
     parent_friendly_level: aiReview.parent_friendly_level || "鞈?銝雲",
diff --git a/index.html b/index.html
index b27dd37..d858038 100644
--- a/index.html
+++ b/index.html
@@ -85,7 +85,7 @@
 
         <!-- Map Container (Mini Map) -->
         <div id="map-container" class="results-container" style="padding-top: 1rem; padding-bottom: 0; height: auto; min-height: unset;">
-            <div style="font-size: 0.85rem; color: var(--text-muted); margin-bottom: 0.75rem; text-align: center; font-weight: 600;">?桀?憿舐內頛?葆撠酋??撱?/div>
+            <div id="map-status" style="font-size: 0.85rem; color: var(--text-muted); margin-bottom: 0.75rem; text-align: center; font-weight: 600;">?桀?憿舐內頛?葆撠酋??撱?/div>
 
             <div id="map" style="height: 250px; min-height: 250px;"></div>
         </div>
diff --git a/style.css b/style.css
index e540fe5..41022e8 100644
--- a/style.css
+++ b/style.css
@@ -1,4 +1,4 @@
-嚜?root {
+:root {
     --primary: #FF7E67;
     --primary-light: #FF9B8A;
     --secondary: #4FB3AA;
@@ -218,8 +218,11 @@ h1 {
     border-radius: 50%;
 }
 
-.legend-dot.confirmed { background: #4FB3AA; }
-.legend-dot.potential { background: #FFB347; }
+.legend-dot.high { background: #15803d; }
+.legend-dot.medium { background: #86efac; }
+.legend-dot.attention { background: #ef4444; }
+.legend-dot.info { background: #94a3b8; }
+
 
 /* Map Info Window Styling */
 .leaflet-popup-content-wrapper {
@@ -841,91 +844,162 @@ h1 {
     padding-top: 0.5rem;
 }
 
-/* --- New UI Styles --- */
+/* --- Tiered Results UI --- */
 
-.quick-filters-container {
-    background: var(--bg-color);
-    padding: 1.5rem 1.5rem 0.5rem;
-    position: sticky;
-    top: 0;
-    z-index: 101;
+.results-section {
+    margin-bottom: 2.5rem;
+    animation: fadeIn 0.4s ease-out;
 }
 
-.quick-filters-label {
+.section-header {
+    font-size: 1.15rem;
     font-weight: 800;
-    font-size: 0.95rem;
     color: var(--text-main);
-    margin-bottom: 0.75rem;
-}
-
-.quick-filters-scroll {
+    margin: 2rem 0 1rem 0;
+    padding-left: 0.75rem;
+    border-left: 5px solid var(--primary);
     display: flex;
+    align-items: center;
     gap: 0.5rem;
-    overflow-x: auto;
-    scrollbar-width: none; /* Firefox */
-    padding-bottom: 0.5rem;
 }
 
-.quick-filters-scroll::-webkit-scrollbar {
-    display: none; /* Chrome/Safari */
+.others-section-header {
+    border-left-color: #CBD5E1;
+    color: #64748B;
+    font-size: 1rem;
+    margin-top: 1.5rem;
 }
 
-.quick-chip {
-    flex: 0 0 auto;
-    padding: 0.5rem 0.8rem;
-    background: #FFFFFF;
-    border: 1px solid #E2E8F0;
-    border-radius: 2rem;
-    font-size: 0.85rem;
-    font-weight: 600;
-    color: var(--text-muted);
-    cursor: pointer;
-    box-shadow: 0 1px 2px rgba(0,0,0,0.05);
-    transition: var(--transition);
+/* Collapsible Content */
+.collapsible-container {
+    overflow: hidden;
 }
 
-.quick-chip.active {
-    background: var(--secondary);
-    color: white;
-    border-color: var(--secondary);
+.collapsible-content {
+    display: none;
 }
 
-.decision-reason {
+.collapsible-content.expanded {
+    display: block;
+}
+
+.show-more-btn {
+    width: 100%;
+    padding: 1.125rem;
+    margin: 1.5rem 0;
+    background: #F8FAFC;
+    border: 2px dashed #E2E8F0;
+    border-radius: 1rem;
+    color: #475569;
+    font-size: 0.95rem;
+    font-weight: 700;
+    cursor: pointer;
     display: flex;
+    justify-content: center;
     align-items: center;
-    gap: 0.5rem;
-    margin-bottom: 0.5rem;
+    gap: 0.6rem;
+    transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
+}
+
+.show-more-btn:hover {
+    background: #F1F5F9;
+    border-color: #CBD5E1;
+    color: var(--text-main);
+}
+
+.show-more-btn .arrow-icon {
+    font-size: 0.8rem;
+    transition: transform 0.3s;
+}
+
+.show-more-btn.active .arrow-icon {
+    transform: rotate(180deg);
+}
+
+/* Enhanced Restaurant Card */
+.card-summary {
+    font-size: 0.85rem;
+    color: #64748b;
+    margin: 0.75rem 0;
+    line-height: 1.6;
+    background: #fcfdfe;
+    padding: 0.75rem;
+    border-radius: 0.75rem;
+    border-left: 2px solid #f1f5f9;
+}
+
+.card-tags {
+    display: flex;
     flex-wrap: wrap;
+    gap: 0.4rem;
+    margin: 0.75rem 0;
 }
 
-.level-badge {
-    padding: 0.2rem 0.6rem;
+.decision-summary {
+    display: inline-flex;
+    align-items: center;
+    padding: 0 0.6rem;
     border-radius: 0.5rem;
-    font-size: 0.75rem;
+    font-size: 0.8rem;
     font-weight: 800;
-    color: white;
+    height: 28px;
+    line-height: 26px;
+    box-sizing: border-box;
+    white-space: nowrap;
 }
 
-.level-high { background: #4FB3AA; }
-.level-mid { background: #FFB347; }
-.level-low { background: #CBD5E1; color: #475569; }
+.tag-badge {
+    display: inline-flex;
+    align-items: center;
+    padding: 0 0.5rem;
+    border-radius: 0.5rem;
+    font-size: 0.75rem;
+    font-weight: 700;
+    height: 28px;
+    line-height: 1;
+    box-sizing: border-box;
+    white-space: nowrap;
+    transition: all 0.2s;
+}
 
-.reason-text {
-    font-size: 0.85rem;
-    font-weight: 600;
-    color: var(--secondary);
+/* Feature tags for High/Medium - Blue System */
+.tag-feature {
+    color: #2563EB;
+    background: #EFF6FF;
+    border: 1px solid #DBEAFE;
 }
 
-.distance-badge {
-    background: #F1F5F9;
-    color: #475569;
-    padding: 0.2rem 0.6rem;
-    border-radius: 1rem;
-    font-size: 0.75rem;
+/* Secondary tags for Needs Attention - Gray System */
+.tag-secondary {
+    color: #6B7280;
+    background: #F9FAFB;
+    border: 1px solid #E5E7EB;
+}
+
+.warning-container {
+    margin-top: 0.75rem;
+    display: flex;
+    flex-direction: column;
+    gap: 0.4rem;
+}
+
+.warning-item {
+    display: flex;
+    align-items: center;
+    gap: 0.4rem;
+    color: #E11D48;
+    font-size: 0.85rem;
     font-weight: 700;
 }
 
-/* Fix sticky header */
-.filter-section {
-    padding-top: 0.5rem;
+/* Map Marker Linkage */
+.restaurant-card.highlighted {
+    border-color: var(--primary);
+    box-shadow: 0 0 20px rgba(255, 107, 107, 0.15);
+    transform: scale(1.02);
+}
+
+@keyframes fadeIn {
+    from { opacity: 0; transform: translateY(10px); }
+    to { opacity: 1; transform: translateY(0); }
 }
