# Local raw API responses

Raw Google Places API response JSON files are intentionally not stored in this
repository.

Keep any regenerated `response/*.json` files local only. The static site runs
from `ai_review/index.js`, so this folder is not required for the deployed
frontend.
