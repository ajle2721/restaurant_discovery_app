# Archived Scripts

These scripts record historical repair and migration work. They are not part of
the supported build and may still refer to the pre-refactor root-level
`app.js` or `ai_review/` paths. Do not run them without reviewing and updating
their targets first.
